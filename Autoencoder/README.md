# MNIST Classification and Latent Space Exploration with PyTorch

This repository contains two PyTorch projects using the MNIST handwritten digits dataset, focusing on **supervised classification** and **unsupervised representation learning** through autoencoders.

---

## 📁 Projects Included

### 1️⃣ MNIST Classification and Representation Learning
- Fully connected neural network for digit classification (0–9)
- Trained using Cross Entropy Loss
- Visualizes learned first-layer weight filters
- Includes an autoencoder for image reconstruction and latent feature learning

**Key concepts:** supervised learning, neural networks, feature visualization

---

### 2️⃣ MNIST Autoencoder with 3D Latent Space
- Autoencoder compressing images into a 3D latent space
- 3D visualization of latent representations for train and test data
- Color-coded digit clusters
- Random latent space sampling to generate new digit-like images

**Key concepts:** unsupervised learning, latent space, dimensionality reduction, generative modeling

---

## 🧠 Technologies
- Python
- PyTorch & Torchvision
- NumPy
- Matplotlib


